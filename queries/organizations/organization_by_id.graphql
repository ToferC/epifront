query OrganizationById($id:UUID!) {
  organizationById(id:$id) {
    nameEn
    acronymEn
    orgType
    url
    orgTiers {
      id
      nameEn
      primaryDomain
      owner {
        id
        givenName
        familyName
        email
      }
    }
    getCapabilityCounts {
      domain
      name
      counts
    }
  }
}